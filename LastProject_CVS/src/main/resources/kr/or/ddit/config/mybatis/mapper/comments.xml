<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="comments">

	<insert id="newComments" parameterType="commentsVo">
		INSERT INTO COMMENTS(CM_ID, BD_ID, MEM_ID, cm_content, CM_DATE, CM_DELNY, CM_OPENNY )
		VALUES (COMMENT_NO.nextval,#{bd_id},'admin',DBMS_LOB.SUBSTR(#{cm_content},4000,1),sysdate,'N',#{cm_openny})
    
	</insert>
	
	<select id="getListComments" parameterType="String" resultType="commentsVo">
		SELECT * FROM COMMENTS where bd_id = #{bd_id}
	</select>

</mapper>